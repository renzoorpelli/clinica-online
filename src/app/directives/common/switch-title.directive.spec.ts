import { SwitchTitleDirective } from './switch-title.directive';

describe('SwitchTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new SwitchTitleDirective();
    expect(directive).toBeTruthy();
  });
});
