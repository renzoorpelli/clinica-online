import { ToolTipSpecialityDirective } from './tool-tip-speciality.directive';

describe('ToolTipSpecialityDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolTipSpecialityDirective();
    expect(directive).toBeTruthy();
  });
});
