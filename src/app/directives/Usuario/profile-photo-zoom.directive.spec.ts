import { ProfilePhotoZoomDirective } from './profile-photo-zoom.directive';

describe('ProfilePhotoZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfilePhotoZoomDirective();
    expect(directive).toBeTruthy();
  });
});
