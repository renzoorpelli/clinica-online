import { CaptchaValidatorDirective } from './captcha-validator.directive';

describe('CaptchaValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CaptchaValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
