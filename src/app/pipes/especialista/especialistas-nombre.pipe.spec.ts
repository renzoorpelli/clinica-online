import { EspecialistasNombrePipe } from './especialistas-nombre.pipe';

describe('EspecialistasNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new EspecialistasNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
