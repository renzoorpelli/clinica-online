import { EspecialistaPendientePipe } from './especialista-pendiente.pipe';

describe('EspecialistaPendientePipe', () => {
  it('create an instance', () => {
    const pipe = new EspecialistaPendientePipe();
    expect(pipe).toBeTruthy();
  });
});
