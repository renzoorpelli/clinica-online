import { FilterShiftBySpecialistPipe } from './filter-shift-by-specialist.pipe';

describe('FilterShiftBySpecialistPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterShiftBySpecialistPipe();
    expect(pipe).toBeTruthy();
  });
});
