import { FilterShiftBySpecialityPipe } from './filter-shift-by-speciality.pipe';

describe('FilterShiftBySpecialityPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterShiftBySpecialityPipe();
    expect(pipe).toBeTruthy();
  });
});
