import { PacientePendientePipe } from './paciente-pendiente.pipe';

describe('PacientePendientePipe', () => {
  it('create an instance', () => {
    const pipe = new PacientePendientePipe();
    expect(pipe).toBeTruthy();
  });
});
